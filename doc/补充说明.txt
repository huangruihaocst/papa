1. 源代码
	本系统所有的源代码均在
		../app/
		../db/
	两个目录里.

2. 文档
	本套系统的需求文档, 设计文档分为两份, 均有 docx 和 pdf 格式, 位于本文件所在目录下.
	本套系统的 web, app 使用手册分为两份, 均为 docx 和 pdf 格式, 位于本文件所在目录下.

	数据库端单独列了一份详尽的文档, 为 md 格式, 根文档为 ../db/doc/index.md

	本套系统最最主要的, 核心的评分记录等逻辑都在数据库模块里, 因此我们主要对数据库端进行了评测.
	本套系统数据库端的测试文档为 html 格式, 位于 ../db/coverage 目录下. 覆盖率 92.13%.
	
	使用视频在十五周曾发送过给您. 为了节省流量, 这次我们就不发了. 可以吗?

3. 配置
	数据库的配置详见 ../db/doc/overview.md
	我们已经配置好了一台服务器, IP: 45.32.52.110 端口 80
	
	app 的项目目录位于 ../app/kernel/
	可用 Andorid Studio 1.4 编译
	我们已经编译好了一份二进制代码 位于 ../app/bin/papa.apk 可以在 android(>= 4.0) 手机上安装使用
	它会主动连接服务器 45.32.52.110

4. 分工 
	项目组长:雷凯翔
	数据库端组员:王奥丞(同时定义了全套接口, = 架构师?)
	app 端组员: UI 相关黄锐皓, 后台网络访问相关 雷凯翔
	web 端组员:丁铭、罗干

5. 编码
	app 端使用的编码标准为 http://source.android.com/source/code-style.html
	数据库端使用的标准为 rails 规范.

